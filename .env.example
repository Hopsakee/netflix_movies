# Copy to .env on the deploy host. NEVER commit a populated copy.
# TMDB Bearer token — https://www.themoviedb.org/settings/api
TMDB_API_KEY=
# Session cookie signing key — generate with: openssl rand -hex 32
SESSION_SECRET=
